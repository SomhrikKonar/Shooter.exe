export interface crosshairProps {
    mouseState:{ x: number; y: number,state:boolean }
}
export interface defaultCrosshair{
    offset:number,
    length:number,
    color:string,
    thickness:number
}